import { ChartouppercasePipe } from './chartouppercase.pipe';

describe('ChartouppercasePipe', () => {
  it('create an instance', () => {
    const pipe = new ChartouppercasePipe();
    expect(pipe).toBeTruthy();
  });
});
